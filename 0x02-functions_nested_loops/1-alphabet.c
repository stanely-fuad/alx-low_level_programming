#include "main.h"
#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * print_alphabet - prints alphabet
 * Return: Always 0 (success)
 */
void print_alphabet(void)
{
char a;
for (a = a; a <= z; a++)
{
putchar(a);
}
putchar(n);
return (0);
}
